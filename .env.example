# Environment Configuration
NODE_ENV=production
PORT=4000

# Firebase Service Account
# Coloque o conteúdo do firebase-service-account.json em uma única linha
# ou use Railway's file storage para arquivos sensíveis
